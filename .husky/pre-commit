#!/bin/sh

echo "🔍 Running Next.js type and lint checks..."
if ! npm run lint 2>&1; then
  echo "❌ Type or lint errors found. Fix them before committing."
  exit 1
fi

echo "✅ All checks passed!"
exit 0
